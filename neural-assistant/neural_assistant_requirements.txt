# Neural Assistant - Production Requirements
# Python >=3.10,<3.13

# ============================================================================
# CORE FRAMEWORK (required)
# ============================================================================

# Web framework and API
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
websockets>=12.0,<14.0
pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0

# HTTP client (used by OllamaProvider and internal calls)
httpx>=0.26,<1.0

# Async utilities
aiofiles>=23.0,<25.0

# Configuration
pyyaml>=6.0,<7.0
python-dotenv>=1.0,<2.0
python-multipart>=0.0.6,<1.0

# Math / array operations (core dependency)
numpy>=1.24,<3.0

# ============================================================================
# LLM PROVIDER SDKs (install the ones you use)
# ============================================================================

# OpenAI (GPT-4, GPT-4o, embeddings)
openai>=1.30,<3.0

# Anthropic (Claude)
anthropic>=0.40,<1.0

# Google Generative AI (Gemini)
google-generativeai>=0.5,<1.0

# Ollama uses httpx (already listed above)
# No separate package needed — OllamaProvider calls the REST API directly

# Local LLM (in-process inference — no external server)
llama-cpp-python>=0.3,<1.0
huggingface-hub>=0.20,<1.0

# ============================================================================
# EMBEDDINGS (optional but recommended)
# ============================================================================

# Sentence embeddings — used by EmbeddingService
# Falls back to TF-IDF bag-of-words if not installed
sentence-transformers>=2.2,<4.0
torch>=2.1,<3.0

# ============================================================================
# MULTIMODAL PROCESSING (optional)
# ============================================================================

# Image processing
Pillow>=10.0,<12.0

# OCR (requires Tesseract binary installed on system)
# pytesseract>=0.3,<1.0

# Document processing
PyPDF2>=3.0,<4.0
python-docx>=1.0,<2.0
openpyxl>=3.1,<4.0

# Native image generation (local GPU — requires NVIDIA GPU)
diffusers>=0.25,<1.0
accelerate>=0.25,<1.0
safetensors>=0.4,<1.0

# Speech-to-text (local — requires FFmpeg for video)
openai-whisper>=20231117
# torchaudio>=2.1,<3.0  # Optional, for advanced audio processing

# ============================================================================
# DATABASE & STORAGE (production)
# ============================================================================

sqlalchemy>=2.0,<3.0
psycopg2-binary>=2.9,<3.0
redis>=5.0,<6.0
alembic>=1.13,<2.0

# ============================================================================
# MONITORING & LOGGING (production)
# ============================================================================

loguru>=0.7,<1.0
structlog>=23.0,<25.0
prometheus-client>=0.19,<1.0
psutil>=5.9,<7.0

# OpenTelemetry (optional distributed tracing)
# opentelemetry-api>=1.21,<2.0
# opentelemetry-sdk>=1.21,<2.0

# ============================================================================
# SECURITY (production)
# ============================================================================

# Password hashing
bcrypt>=4.0,<5.0

# JWT authentication
PyJWT>=2.8,<3.0

# ============================================================================
# TESTING & CODE QUALITY (dev only — install with: pip install -r requirements-dev.txt)
# ============================================================================

# pytest>=7.4,<9.0
# pytest-asyncio>=0.21,<1.0
# pytest-cov>=4.1,<6.0
# pytest-mock>=3.12,<4.0
# black>=23.0,<25.0
# isort>=5.12,<6.0
# flake8>=6.0,<8.0
# mypy>=1.7,<2.0
# bandit>=1.7,<2.0
