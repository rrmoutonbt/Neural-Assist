# Neural Assistant - Local LLM Dependencies
# Install with: pip install -r requirements-local.txt

# The pure Python GGUF engine (gguf_engine.py) requires ONLY numpy,
# which is already in the base requirements. No additional packages needed.

# Optional: llama-cpp-python for faster GPU-accelerated inference
# Falls back to PureGGUFBackend if not installed
# llama-cpp-python>=0.3,<1.0

# Optional: HuggingFace model downloads (can also download manually)
# huggingface-hub>=0.20,<1.0

# Optional: HuggingFace Transformers backend (GPU recommended)
# torch>=2.1,<3.0
# transformers>=4.36,<5.0
# accelerate>=0.25,<1.0
